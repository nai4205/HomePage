export interface Options {
    id: number
    snapType: string;
    gridSizeWidth: number;
    gridSizeHeight: number;
    canvasWidth: number;
    canvasHeight: number;
}

export const options : Options[] = [
    {
    id: 0,
    snapType: 'float',
    gridSizeWidth: 10,
    gridSizeHeight: 10,
    canvasWidth: 0,
    canvasHeight: 0
    },

]