export interface Options {
    id: number
    snapType: string;
    gridSizeWidth: number;
    gridSizeHeight: number;
}

export const options : Options[] = [
    {
    id: 0,
    snapType: 'float',
    gridSizeWidth: 10,
    gridSizeHeight: 10,
    }
]